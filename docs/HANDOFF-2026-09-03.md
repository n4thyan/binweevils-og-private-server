# HANDOFF — 2026-09-03 (Bin Pets + Loyalty Card)

## Bin Pets: DONE / ready for gameplay verification

- Full Bin Pets PHP endpoint set under `game-full/php2/pets/` and `php/`.
- Pet helper functions in `game-full/essential/internal.php`.
- SmartFox pet handlers in `server/BinWeevils.js` and `server/Weevil.js`.
- DB migration committed: `migrations/bin-pets-2026-09-03.sql`.
- Branch: `feature/bin-pets-integration` @ `85c5a74e`.

## Loyalty Card: DONE / ready for client playtest

- Loyalty backend restored: `getProgress`, `getStamp`, `getVouchers`, `finalReward`.
- DB schema + 480 seed rows applied: `loyalty_cards`, `loyalty_vouchers`, `loyalty_card_rewards`.
- Runtime SWF: `cdn.binw.net/externalUIs/loyaltyCard_28_11_13.swf` (repo + htdocs identical, MD5 `59851414e306641c5bd17527252ba233`).
- Original UI trigger present in current client build — no custom entry point invented.
- HTTP blocker diagnosed and fixed: missing file sync + `bind_param` type fix in `getStamp.php`.
- Docs: `docs/LOYALTY-CARD-RECOVERY-2026-09-03.md`.

## Do NOT touch yet

- XP shop, player-card backdrop, level-star cosmetics, Lotto, VPS migration,
  unrelated endpoint repairs, puzzle minigames.
