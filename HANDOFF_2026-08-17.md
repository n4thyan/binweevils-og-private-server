# END-OF-SESSION HANDOFF — Bin Weevils Private Server (Bin Pets in progress)
Generated: 2026-08-17  (session after ~7.5h dev; backend + partial Bin Pets)
This file is a STATE EXTRACTION. Nothing was changed, committed, deleted, or refactored while
producing it. All claims are grounded in files currently on disk. Where the earlier-pasted
narrative or ROADMAP.md CONTRADICTS the source, the SOURCE wins (flags below).

================================================================================
1. ORIGINAL SOURCE
================================================================================
- Original repository: KnowYourKnot/Binweevils (referred to in docs as
  "KnowYourKnot/Binweevilsworks"). Used 1:1; architecture preserved.
- Exact branch/commit: NOT RECORDED in this session's artifacts. The baseline
  zip (Binweevils-main (1).zip, 1.41 GB) is dated 2026-08-17 11:02 and its
  extracted copy (2) is dated Jul 5 2021 on every file => the source is the
  Jul-2021-era KnowYourKnot tree.
- Current project STILL derives from that clean KnowYourKnot source (only the
  active copy (1) has been modified; (2), (3) and the three zips are untouched).
- Previous Bin Weevils projects imported? NO. The memory-referenced
  C:\Users\pc\Desktop\Binweevils (separate git repo) and Binweevils-OG
  (original Flash Electron stack) were NOT merged into this working copy.
  This working copy is independent of both.

================================================================================
2. CURRENT PROJECT LOCATION
================================================================================
ACTIVE WORKING COPY (the one that was being edited):
  C:\Users\pc\Desktop\Project Binweevils\Binweevils-main (1)\Binweevils-main\

TWO PRISTINE REFERENCE COPIES (DO NOT TOUCH):
  C:\Users\pc\Desktop\Project Binweevils\Binweevils-main (2)\Binweevils-main\   (Jul-2021 baseline)
  C:\Users\pc\Desktop\Project Binweevils\Binweevils-main (3)\Binweevils-main\   (Jul-2021 baseline)

THREE ORIGINAL ZIP BACKUPS (DO NOT TOUCH):
  C:\Users\pc\Desktop\Project Binweevils\Binweevils-main (1).zip   (1.41 GB)
  C:\Users\pc\Desktop\Project Binweevils\Binweevils-main (2).zip   (1.41 GB)
  C:\Users\pc\Desktop\Project Binweevils\Binweevils-main (3).zip   (1.41 GB)
  (also a 2.8 GB "Bin Weevils Game Assets (1).zip" + an extracted "Bin Weevils Game Assets (1)" dir,
   and a "Bin Weevils Game Assets (2)- Copy" dir — raw asset dumps, untouched.)

VERIFIED: the (2)/(3) copies and all zips were NOT modified this session.
The active copy differs from (2) ONLY in the files enumerated in section 3.

================================================================================
3. COMPLETE CHANGE SUMMARY (ACTIVE COPY vs PRISTINE (2), source-only / excludes node_modules)
================================================================================
NEW FILES ADDED (verified by directory diff):
  game-full/essential/admin_guard.php                  — mod-panel gate (enforceAdminAccess + modToken)
  game-full/php2/pets/adoptPet.php                     — Bin Pet adoption endpoint (hash-based contract)
  game-full/php2/pets/buy.php                          — Bin Pet adoption endpoint (session-cookie contract, incl. debug log write)
  game-full/php2/pets/feedPet.php                      — Bin Pet care (feed/play/train) endpoint
  game-full/php2/pets/validate-pet-name.php            — Bin Pet name validation endpoint
  game-full/php2/pets/buy_post_debug.log               — captured live SWF POSTs (adoptPet contract: userIDX/hash/timer/itemTypeID/petName)
  ROADMAP.md                                           — future-work list (see CONTRADICTION flag below)
  CHECKPOINT_A_ADMIN_CREDS.txt                         — dev admin accounts (Rick/Raving/Sludge, isModerator=1)
  reset_admins_checkpointA.sql                         — admin reset
  bwps.sql.backup-20260817_114118                      — pre-edit DB dump
  server/roomchange_debug.log                          — captured live roomChange packets (proves BinPetsShop reached)
  NUL                                                  — ANOMALY: a 37 KB file literally named "NUL" containing an HTML doc
                                                          titled "BWR Admin". Likely an accidental redirect/asset write; flag it, do NOT trust it.
  ~80 SWF asset files under game-full/cdn.binw.net/...  — mostly Bin Pet / Bin Pets Paradise / campaign SWFs;
                                                          these are ORIGINAL GAME ASSETS present in the dump, not authored this session.
                                                          (They exist in the source dump; only surfaced in the active copy because the dump was
                                                          extracted there. Treat as source assets, not new work.)

FILES THAT DIFFER FROM PRISTINE (md5 diff, excludes node_modules):
  bwps.sql                         — modified (see DB section). Only DB-schema seed/content differs; the `pets`
                                      and `petacquiredskills` tables were ALREADY in the Jul-2021 baseline (see §6).
  electron/main.js                 — modified (Aug 17 12:31). Diff vs baseline is small; it is the PepperFlash/
                                      Electron launcher. NOT substantially reworked; treat as KNOWN-GOOD / frozen.
  electron/package-lock.json       — changed because node_modules was (re)installed.
  game-full/848fjogfndsl/*         — the admin panel; ~13 files differ (index.php + panel/apis/* + admintools.js).
                                      These implement modToken-gated moderation (kick/ban/warn/reward/devtask APIs).
                                      KNOWN-GOOD for moderation; do not touch unless extending moderation.
  game-full/essential/internal.php — MODIFIED (Aug 17 16:37, 4634 lines). Bin Pet shop plumbing:
                                      getNestShopItems() now special-cases storeName 'binPetShop' (line 990-1008),
                                      and the nest purchase count query references 'binPetShop' (line 1101).
                                      This is the Bin Pet PHP entry that was being wired.

SUBTREES UNCHANGED vs PRISTINE:
  server/Weevil.js, server/BinWeevils.js, server/Main.js, server/server.js, server/db.js —
  the TCP/WebSocket game server logic is the Jul-2021 baseline + Checkpoint B softDrop edits
  (see §4). NO Bin Pet logic was added to the server (confirmed: grep for pet in Weevil.js/BinWeevils.js
  returns networking-noise only, 0 Bin Pet references).

================================================================================
4. BACKEND CHECKPOINT STATUS (verified against source, NOT the pasted narrative)
================================================================================
NOTE ON SOURCE-OF-TRUTH CONFLICT:
  The pasted "first prompt" block says A7/A8 + D8–D11 are NOT DONE.
  ROADMAP.md (generated this session) claims A7/A8 + D8–D11 WERE closed this pass.
  VERIFIED SOURCE shows a MIX. The source is authoritative.

CHECKPOINT A — Authentication / account security
  A1  bcrypt hashing ................ IMPLEMENTED  (register/create-new-weevil.php:67 password_hash)
  A2  param session UPDATE .......... IMPLEMENTED  (login.php prepared UPDATE + bind_param)
  A3  strong key gen ................ IMPLEMENTED  (generateLogKey 24B, generateSessionKey 32B, random_bytes)
  A4  TCP login rate limit .......... IMPLEMENTED  (Weevil.js tcpLoginAttempts Map)
  A5  secrets centralised .......... IMPLEMENTED  (config.php bw_env() + .env fallbacks)
  A6  register->game.php ............ IMPLEMENTED  (login.php header Location ../game.php)
  A7  create-weevil rate limit ...... IMPLEMENTED (CONTRADICTS old block)
                                      (register/create-new-weevil.php:91 rateLimit('create-weevil',5,600))
  A8  web login rate limit .......... IMPLEMENTED (CONTRADICTS old block)
                                      (login/login.php:77 rateLimit('web-login',10,300))
  A9  CAPTCHA infra (off) ........... IMPLEMENTED  (config.php RECAPTCHA_*, guarded in create-new-weevil.php)
  A10 username validation .......... IMPLEMENTED  (isValidUsername regex)
  A11 IP privacy (HMAC) ............. IMPLEMENTED  (loginIP/regIP stored as HMAC; IP_HASH_SECRET in config.php)
  Automated tests: NONE in repo. Manual: developer typed-confirmed per pasted block.
  Remaining issues: none blocking.

CHECKPOINT B — Invalid packet / kick refactor
  softDrop + 60s/10-violation ...... IMPLEMENTED  (Weevil.js per-conn counter, threshold)
  dropOnly (misparse) .............. IMPLEMENTED
  mod kick/ban preserved ........... IMPLEMENTED  (adminKickUser/adminBanUser untouched)
  State: IMPLEMENTED + syntax-checked. Not unit-tested this session; behaviour observed via roomchange_debug.log
        (server accepted/joined real room packets).

CHECKPOINT C — Chat
  profanity->asterisks broadcast .... IMPLEMENTED (per pasted block, verified via real-class unit test earlier)
  ]]> CDATA escape .................. IMPLEMENTED
  special chars usable ............. IMPLEMENTED (only < stripped)
  State: IMPLEMENTED.

CHECKPOINT D — Hardening / quality  (MIXED — ROADMAP.md over-claims)
  Panel locked behind modToken ...... IMPLEMENTED  (enforceAdminAccess across 848fjogfndsl panel; admin_guard.php)
  SQLi LIMIT/OFFSET param ........... IMPLEMENTED  (internal.php prepared stmts)
  error-leak removed ................ IMPLEMENTED  (display_errors off, dead var_dumps gone)
  D8  unified moderation func ....... NOT IMPLEMENTED (CONTRADICTS ROADMAP.md)
                                      Weevil.js still has 3 separate funcs:
                                        adminBanUser   (line 2415)
                                        adminKickUser  (line 2467)
                                        adminWarnUser  (line 2494)
  D9  packet history ~200 ........... IMPLEMENTED
                                      Weevil.js:92-93,138-139 capped 200-entry ring buffer
  D10 generic per-action rate limit . PARTIAL — chat spam only. No hat/nest/spin limits.
                                      rateLimit() helper exists (backbone.php:31) and is used by
                                      create-weevil, web-login, adopt-pet, care-pet. NOT applied broadly.
  D11 movement/coord validation ..... NOT IMPLEMENTED (CONTRADICTS ROADMAP.md)
                                      BinWeevils.js:416 only a COMMENT at the movement-packet site;
                                      no coord clamping/validation logic.
  D12 upsert ........................ PARTIAL — a couple ON DUPLICATE in internal.php + pets code.
  D13 userID once/request ........... NOT IMPLEMENTED.
  State: PARTIAL. ROADMAP.md's assertion that D8/D11 were closed this pass is FALSE per source.

================================================================================
5. LOCALHOST STATUS (verified from live process + debug log, not inference)
================================================================================
RUNNING NOW (tasklist at session end):
  mysqld.exe        (PID 15220)  — MariaDB/MySQL (XAMPP) on localhost
  httpd.exe  x2     (PID 14472, 2436) — Apache (XAMPP) serving game-full
  node.exe          (PID 15128)  — `node Main.js` (C:\Program Files\nodejs\node.exe Main.js)
                                    => TCP game server (BinWeevils :9339) + web (BinWeevilsWeb :2087)
                                    + WebSocket (server.js :10843)

REAL-WORLD PATH THAT WAS EXERCISED:
  Web login (Apache/PHP) -> session key -> Electron (PepperFlash) -> TCP :9339 -> DB (bwps) -> game.
  CONFIRMED LIVE: server/roomchange_debug.log shows a weevil ("nest_Rick") traversing
    nest_Rick -> ShoppingMall -> ShoppingMallInside -> BinPetsShop (room id 314 / 134)
  at 16:53–17:05 on 2026-08-17. So: registration works, login works, game login works,
  the client reaches the game, rooms work, and the BinPetsShop room was physically entered.

WHAT IS KNOWN TO WORK (observed):
  - Registration, web login, game/Electron launch, TCP join, room changes (incl. BinPetsShop).
  - Bin Pet SHOP UI loads (client navigated into BinPetsShop).
  - Bin Pet adoption HTTP traffic occurred: php2/pets/buy_post_debug.log captured 4+ real POSTs
    from the SWF (userIDX=1, hash=x, timer=1, itemTypeID=2846, petName=Spud) on 2026-08-17 ~19:00–19:04.
    => the SWF is calling an adoption endpoint live.

WHAT IS UNKNOWN / NOT VERIFIED THIS SESSION:
  - Whether a pet row was actually INSERTED (no DB read possible: mysql client not on PATH,
    php CLI not on PATH in this MSYS shell). The debug log only proves the POST arrived;
    it does NOT prove the INSERT succeeded or that the SWF consumed the response.
  - Whether the Bin Pet renders/animates in-world after adoption.

CURRENT FAILURE/UNKNOWN STATE:
  - The Bin Pet SHOP (in-game nest-shop query) returns catalog from itemtype WHERE shopType='binPetShop'.
    VERIFIED: there are ZERO rows in bwps.sql with shopType='binPetShop' (grep -c binPetShop => 0).
    => The getNestShopItems() binPetShop branch will return an EMPTY catalog until seed items are added.
    This is the most likely reason the shop appears empty / adoption cannot pick a real species.
  - adoptPet.php and buy.php are TWO DIVERGENT adoption endpoints with different POST contracts
    (adoptPet expects hash/idx/itemTypeID/petName/bc/ac1/ac2/ec1/ec2; buy expects name/bc/ac1/ac2/ec1/ec2/
    bowlItemTypeId/bedColour, no hash). The live SWF used the adoptPet contract (per debug log).
    The two files duplicate logic and may conflict — pick ONE as canonical.

================================================================================
6. DATABASE STATE
================================================================================
- DB name: bwps (localhost, XAMPP, root / no password — dev default).
- Schema: MySQL dump bwps.sql (8767 lines). Tables `pets` (line 3794) and `petacquiredskills`
  (line 3779) are part of the ORIGINAL Jul-2021 baseline — they were NOT created this session.
  `pets` columns: id, ownerID, name, bedID, bowlID, bc, ac1, ac2, ec1, ec2, fuel, mentalEnergy,
  health, fitness, experience, adoptedDate, nameHash.
- Migrations performed: NONE structural. bwps.sql differs from baseline only in seed/content rows
  (no new tables/columns added this session). The `pets` table has ZERO seeded rows in the dump.
- Important missing data: NO itemtype rows with shopType='binPetShop' exist (the Bin Pet Shop
  catalog is empty). Bin Pet bowl items DO exist: f_petBowl_blue/black/white (itemTypeID 2625/2626/2627,
  category 'bin pet stuff', canBuy=0). Bin Pet bed items exist: itemTypeID 2855–2863 ('Bin Pet Bed ...').
- Seed/test accounts: dev admins Rick / Raving / Sludge (isModerator=1) — see CHECKPOINT_A_ADMIN_CREDS.txt.
- DB wiped/recreated? A reset_admins_checkpointA.sql exists; a bwps.sql.backup-20260817_114118 pre-edit
  dump exists. The live DB state at session end is NOT confirmed via query (no mysql/php CLI here).
- Backups: yes — bwps.sql.backup-20260817_114118 + the 3 pristine zips + copies (2)/(3).
- Known DB issues:
  (a) Empty binPetShop catalog (no shopType='binPetShop' rows) => shop shows nothing.
  (b) adoptPet.php hard-rejects any item whose shopType != 'binPetShop' (line 37) — with empty
      catalog this always fails, so the live adoption POSTs likely returned res=998 / never inserted.

================================================================================
7. BIN PETS — CURRENT WORK  (most important continuation section)
================================================================================
GOAL:
  Implement Bin Pets: a player adopts a customised pet (body/accessory/eye colours + name) from the
  Bin Pet Shop, the pet is stored per-owner in the `pets` table, and the player can feed/play/train it
  (fuel/health/fitness/experience) and view skills (petAcquiredSkills). The Bin Pet Shop is a nest-shop
  storeName='binPetShop'; adoption is completed by a petBuilder SWF that POSTs to a PHP endpoint.

SOURCE / ASSETS BEING USED:
  - Schema: existing `pets` + `petacquiredskills` tables (baseline, latin1, NOT re-created).
  - PHP helpers already in game-full/essential/backbone.php: checkHash(), getAllWeevilStatsByName(),
    removeMulch(), removeDosh(), addExperience(), getItemDataById(), rateLimit() (line 31).
  - internal.php: getNestShopItems($tag,$storeName) already special-cased for 'binPetShop' (line 990).
  - Client: petBuilder / BinPetsShop SWFs already present in the asset dump (cdn.binw.net/fixedCam/BinPetsShop*.swf,
    play/fixedCam/BinPetsShop.swf, externalUIs/BinPetGuideBook.swf, etc.).
  - Bowl/bed items (2625–2627 bowls, 2855–2863 beds) already in itemtype.

FILES CHANGED (Bin Pets specific):
  game-full/php2/pets/adoptPet.php        (NEW)
  game-full/php2/pets/buy.php             (NEW, divergent contract + debug log)
  game-full/php2/pets/feedPet.php         (NEW)
  game-full/php2/pets/validate-pet-name.php (NEW)
  game-full/php2/pets/buy_post_debug.log  (NEW, captured live POSTs)
  game-full/essential/internal.php        (MODIFIED: binPetShop branch in getNestShopItems + count query)
  (NO changes to server/Weevil.js, server/BinWeevils.js, or any SWF.)

CODE IMPLEMENTED:
  - adoptPet.php: validates hash+session, rate-limits (adopt-pet 10/300s), checks item is a buyable
    binPetShop item, validates name length (2–16), caps 12 pets/owner, deducts mulch/dosh, INSERTs a
    `pets` row with bc/ac1/ac2/ec1/ec2 + fuel/health/fitness=100/0, grants achievement id 2, returns petID.
  - buy.php: session-cookie auth (NO hash), captures POST to debug log, resolves bowl (2625-base) and
    matching-colour bed (2855-base) item ids, rate-limits, deducts 5000 mulch, grants bowl+bed to
    inventory via grantRewardItem(), INSERTs `pets` row, grants achievement 2.
  - feedPet.php: hash+session auth, rate-limit (care-pet 20/60s), ownership check on `pets`,
    applies feed/play/train deltas (clamped 0–100 for bars), UPDATEs pet, returns new stats.
  - validate-pet-name.php: length 2–16, non-empty, rejects duplicate name for the owner (case-insensitive).
  - internal.php getNestShopItems(): returns ALL canBuy=1 items where shopType='binPetShop' (ignores category tag).

SERVER SIDE (TCP):
  NOTHING implemented. Weevil.js / BinWeevils.js contain zero Bin Pet handling. Pets are stored via PHP
  and (presumably) rendered by the client SWF; no real-time pet sync over the TCP server exists yet.

CLIENT SIDE:
  No SWF/AS changes. The existing BinPetsShop + petBuilder SWFs are used as-is. They call the PHP
  endpoints above. The SWF's expected response format (per buy.php comments) is "error=0" on success
  and "ERROR:<code>" on failure; success echo is "error=0&petID=...&mulch=...&dosh=...&xp=...".

DATABASE:
  `pets` table used as-is (baseline). No new columns/migrations. NO seed rows (pets empty in dump).
  NO binPetShop catalog rows (itemtype). Bowl/bed items present but canBuy=0 (delivered at adoption).

PROTOCOL / PACKETS:
  No new TCP packets/events implemented. Bin Pets ride entirely on HTTP POST to php2/pets/*.php.
  The live SWF POST (adoptPet contract) was: userIDX, hash, timer, itemTypeID, petName.

CURRENT BEHAVIOUR (best reconstruction):
  1. Player opens Bin Pet Shop (BinPetsShop room) — confirmed reachable live.
  2. Shop catalog query runs getNestShopItems('','binPetShop') -> returns EMPTY (no seed rows).
  3. Player (somehow) triggers the petBuilder SWF, which POSTs adoption fields to php2/pets/adoptPet.php
     (or buy.php). Live debug log shows adoptPet-contract POSTs (itemTypeID=2846, petName=Spud).
  4. adoptPet.php line 37 rejects because itemTypeID 2846 is NOT shopType='binPetShop'
     (2846 is an auto-increment id, not a Bin Pet shop item) => returns res=998, no pet inserted.

WHAT WORKS (actually verified):
  - The 4 PHP endpoints exist, parse input, auth via session/hash, rate-limit, and run valid SQL
    (syntax-checked by reading; not executed via CLI here).
  - The BinPetsShop room loads and was entered live.
  - The SWF emits adoption POSTs that hit the server (logged).

WHAT DOES NOT WORK (known):
  - Bin Pet Shop catalog is EMPTY (no binPetShop itemtype rows) -> shop shows no adoptable pets.
  - adoptPet.php will reject every real POST because the validated item isn't shopType='binPetShop'
    (no such rows exist; itemTypeID 2846 is unrelated).
  - No pet is confirmed inserted (no DB read possible here; logic path returns res=998 before INSERT).
  - Two divergent adoption endpoints (adoptPet.php vs buy.php) — ambiguous which the SWF should use.
  - No TCP/server-side pet sync, no skills UI wiring beyond getPetSkills() (which itself is unused).

WHAT REMAINS (precise checklist):
  [ ] Decide canonical adoption endpoint (adoptPet.php OR buy.php); delete the other or merge.
  [ ] Seed binPetShop catalog rows in bwps.sql (or make getNestShopItems return existing pet items
      under a different shopType). Without this the shop is empty and adoptPet always rejects.
  [ ] Fix the itemTypeID validation mismatch: either tag real items shopType='binPetShop' OR change
      adoptPet.php to accept the actual item ids the SWF sends.
  [ ] Verify a pet row is actually inserted (run a real adoption, then SELECT from pets).
  [ ] Verify the SWF consumes the success response and the pet appears in-client.
  [ ] Wire getUserPets()/getPetSkills() into a profile/pet-view if needed.
  [ ] Decide if any TCP server-side pet state is required (movement/visibility of pet in-world).

================================================================================
8. BIN PETS ROADMAP / ORIGINAL INTENT  (implemented vs planned)
================================================================================
IMPLEMENTED (code present, syntax-checked):
  - Adoption PHP endpoint(s) writing to `pets`.
  - Pet care (feed/play/train) PHP endpoint updating `pets` stats.
  - Pet name validation PHP endpoint.
  - binPetShop branch in getNestShopItems().

PLANNED BUT NOT IMPLEMENTED:
  - Actual Bin Pet Shop catalog data (seed rows) — shop is currently empty.
  - Confirmed end-to-end adoption (insert + client render).
  - Pet skills training UI / petAcquiredSkills population (getPetSkills exists but nothing writes skills).
  - In-world pet rendering/sync over TCP (server knows nothing of pets).
  - "Extra Bin Pets" / "Extra nest floors" (explicitly deferred in ROADMAP.md).
  - Bin Pets Paradise rooms (bppLandingLeft/Right, bppGym, bppSalon, bppTemple, bppShop, bppPetcotTower
    exist in roomids.txt but are not wired to any pet logic).

================================================================================
9. CURRENT GIT STATE
================================================================================
- NO git in the active working copy (C:\Users\pc\Desktop\Project Binweevils\Binweevils-main (1)\Binweevils-main\
  has no .git). The separate C:\Users\pc\Desktop\Binweevils repo (with .git, .env, migrate-seed-passwords.php)
  is a DIFFERENT, UNRELATED checkout and was NOT used for this work.
- Therefore: no commits made here, no uncommitted/staged tracking. All changes are plain filesystem edits.
- If you want history, create a git repo here and commit the diff against Binweevils-main (2) baseline.

================================================================================
10. CURRENT PROCESSES / PORTS (verified running at session end)
================================================================================
Component        Process / Command                      Port(s)        Verified?
MySQL/MariaDB     mysqld.exe (XAMPP)                    localhost:3306 yes (running)
Apache (PHP)      httpd.exe x2 (XAMPP)                  localhost:80    yes (running)
Node TCP server   node Main.js  (C:\Program Files\nodejs\node.exe Main.js)
                   - BinWeevils  :9339                   yes (running, PID 15128)
                   - BinWeevilsWeb :2087                 yes
WebSocket         server/server.js (inside Main.js tree) :10843  yes (server.js:4)
Electron/PepperFlash  electron/main.js (launcher)         n/a           launched by dev to play

START COMMANDS (as used this session — reproduce exactly):
  - Start XAMPP (Apache + MySQL) via the XAMPP control panel (Windows).
  - Start TCP server:  cd <active copy>\server  (or repo root)  then  node Main.js
    (Main.js launches BinWeevils :9339 + BinWeevilsWeb :2087; server.js adds :10843 WS).
  - Launch game client: open the Electron app (electron/main.js) — PepperFlash 23 ppapi plugin path
    /plugins/pepflashplayer64_23_0_0_162.dll (win32).
NOTE: `mysql` and `php` CLIs are NOT on the MSYS/PATH used by this shell; use the XAMPP
installation's binaries (typically C:\xampp\mysql\bin\mysql.exe, C:\xampp\php\php.exe) for DB queries.

================================================================================
11. DEPENDENCIES
================================================================================
- Node: present (node Main.js ran; exact `node -v` not captured in this shell — check on resume).
- server/ npm deps: discord.js ^12.5.1, express ^4.17.1, isprofanity ^1.4.1, leo-profanity ^1.3.0,
  line-reader, moment, mysql ^2.18.1, node-bash-title, request, string-strip-html, xml2js.
  (node_modules was (re)installed this session — that's why (1) has node_modules and (2) does not.)
- PHP: XAMPP PHP (version not captured; use php -v on resume). Requires mysqli extension.
- MySQL/MariaDB: XAMPP bundle.
- Electron: ^11.1.0 (electron/package.json). Plugin: PepperFlash 23.0.0.162 (win32 dll in electron/plugins/).
- Other: nothing else noted as newly installed beyond node_modules.

================================================================================
12. KNOWN PROBLEMS
================================================================================
P1 — Bin Pet Shop catalog empty
  Location: bwps.sql (no shopType='binPetShop' rows); internal.php getNestShopItems line 990-1008.
  Severity: HIGH (blocks adoption entirely).
  Reproduction: open Bin Pet Shop in client; catalog query returns 0 rows.
  Understanding: seed data never added; getNestShopItems branch is correct but has no source rows.
  Attempted fix: none yet.
  State: OPEN.
  Next action: add binPetShop itemtype rows (or repoint the query) so the shop populates.

P2 — adoptPet.php always rejects real adoptions
  Location: game-full/php2/pets/adoptPet.php:37.
  Severity: HIGH.
  Reproduction: SWF POSTs itemTypeID (e.g. 2846) -> not shopType='binPetShop' -> res=998, no insert.
  Understanding: validation expects binPetShop items that don't exist; itemTypeID 2846 is unrelated.
  Attempted fix: none.
  State: OPEN (root cause tied to P1).
  Next action: align item ids / shopType with what the SWF actually sends (see buy_post_debug.log).

P3 — Two divergent adoption endpoints
  Location: php2/pets/adoptPet.php vs php2/pets/buy.php.
  Severity: MEDIUM (ambiguity / duplicated logic).
  Reproduction: both exist with different POST contracts; SWF used adoptPet contract.
  Understanding: likely evolved from two reverse-engineering passes of the live SWF.
  Attempted fix: none.
  State: OPEN.
  Next action: pick one canonical endpoint; remove/merge the other.

P4 — Anomalous "NUL" file
  Location: <active copy>/NUL  (37 KB, HTML doc titled "BWR Admin").
  Severity: LOW (clutter / possible accidental write).
  Reproduction: present in active copy root; not in pristine (2).
  Understanding: looks like a misdirected output (e.g. `> NUL` gone wrong, or an asset write).
  Attempted fix: none.
  State: OPEN. Do NOT delete blindly — inspect before any cleanup (it may be referenced).

P5 — Checkpoint D over-claimed in ROADMAP.md
  Location: ROADMAP.md (claims D8/D11 closed this pass) vs source (Weevil.js 2415/2467/2494 still 3 funcs;
  BinWeevils.js:416 only a comment for movement validation).
  Severity: DOCUMENTATION (misleads next session).
  State: OPEN.
  Next action: treat ROADMAP.md D-gaps as NOT done; D8/D11 remain TODO.

P6 — No DB read verified this session
  Location: unable to run mysql/php CLI in MSYS shell.
  Severity: VERIFICATION GAP.
  State: OPEN. Next action: use XAMPP php/mysql binaries to confirm pets table state on resume.

================================================================================
13. DEFERRED WORK
================================================================================
Backend:        D8 unified moderation, D11 movement/coord validation, D12/D13 broader,
                graceful disconnect packets, XML builder unification, sockets-as-Maps,
                Weevil.js module splitting (all from ROADMAP.md).
Client:         main.swf audit/cleanup (no authoritative main.swf identified in game-full;
                mainDEV661/662/663 + dated main_*.swf present — diff for custom fixes before deleting).
Gameplay:       Extra Bin Pets, Extra nest floors (explicitly deferred in ROADMAP.md).
Bin Pets:       skills training/population, in-world pet sync, Bin Pets Paradise room wiring,
                confirmed end-to-end adoption + render.
Room events:    SEPARATE code owned by developer — NOT to be written by us (ROADMAP.md §2).
                Migrate into BinWeevils.js 2#5 dispatch when supplied.
Refactors:      see Backend; structural only, not stability.
Security:       D13 (userID once/request), broader D10 rate limits (hat/nest/spin).
UI:             admin panel polish; Bin Pet profile/pet-view wiring.
Deployment:     not started (local XAMPP only).
Other:          cleanup of game-full/gameTOBECHANGED.php (audit before delete per spec).

================================================================================
14. ORIGINAL ROADMAP VS ACTUAL PROGRESS
================================================================================
Task                         Intended                                  Actual state
A1–A11 auth/hashing/IP       Implement secure auth                     DONE (source-verified)
A7 create-weevil rate limit  (old block: NOT DONE)                     DONE (create-new-weevil.php:91)
A8 web-login rate limit      (old block: NOT DONE)                     DONE (login.php:77)
B invalid-packet refactor    softDrop + threshold + preserve mod       DONE (Weevil.js)
C chat filtering/CDATA       asterisks + ]]> escape                    DONE
D panel modToken lock        lock admin panel                         DONE (848fjogfndsl + admin_guard.php)
D8 unified moderation        single moderation func                  NOT DONE (3 funcs remain)
D9 packet history ~200       audit ring buffer                        DONE (Weevil.js:92-93,138-139)
D10 generic rate limit        per-action limits                       PARTIAL (chat + pet + auth only)
D11 movement validation       coord clamping                          NOT DONE (comment only)
D12/D13 upsert/userID        broader hardening                        PARTIAL / NOT DONE
Bin Pets adoption            adopt + store pet                        PARTIAL (endpoints exist; shop empty; no confirmed insert)
Bin Pets care                feed/play/train                          PARTIAL (feedPet.php written; untested live)
Bin Pets shop catalog        populate binPetShop                      NOT DONE (0 seed rows -> empty shop)
Bin Pets in-world sync       TCP pet state                            NOT DONE (server unaware of pets)

================================================================================
15. FILES THAT MUST NOT BE TOUCHED (frozen / known-good)
================================================================================
- Binweevils-main (2)/ and (3)/ and the three *.zip — pristine references. DO NOT MODIFY.
- electron/main.js, electron/plugins/pepflashplayer64_23_0_0_162.dll — PepperFlash launcher, KNOWN-GOOD.
- game-full/848fjogfndsl/* — moderation panel (modToken-gated), KNOWN-GOOD for kick/ban/warn.
- server/Main.js, server/server.js, server/db.js — TCP/WS spine, baseline + B edits, KNOWN-GOOD.
- bwps.sql baseline schema (`pets`, `petacquiredskills`, etc.) — only ADD seed rows; do not drop/alter
  columns without reason.
- game-full/ BinPetsShop / petBuilder SWFs — client assets, use as-is; do not decompile/replace unless
  extending Bin Pets intentionally.

================================================================================
16. IMPORTANT DISCOVERIES (verified, needed for tomorrow)
================================================================================
- The active working copy is Binweevils-main (1)/Binweevils-main, NOT the memory's
  C:\Users\pc\Desktop\Binweevils git repo (that is a separate, unrelated checkout).
- `pets` and `petacquiredskills` tables are BASELINE (Jul-2021), not created this session.
- There are ZERO binPetShop itemtype rows → the Bin Pet Shop catalog is empty by design-gap,
  and adoptPet.php's shopType check therefore rejects everything. This is the central blocker.
- Live SWF adoption POSTs used the adoptPet.php contract (userIDX/hash/timer/itemTypeID/petName),
  itemTypeID 2846, petName "Spud" — captured in php2/pets/buy_post_debug.log.
- The pet bowl items are 2625–2627 (canBuy=0); beds 2855–2863. buy.php maps bowl->matching bed.
- ROADMAP.md over-claims (says D8/D11 done) — SOURCE IS AUTHORITATIVE; D8/D11 remain TODO.
- A stray file literally named "NUL" (37 KB HTML "BWR Admin") sits in the active root — anomaly, do not trust.
- `mysql`/`php` CLIs are NOT on the MSYS shell PATH; use XAMPP's binaries (C:\xampp\...) for DB work.
- The developer launched the REAL live Bin Weevils private-server client once (per pasted note) — but the
  working copy + debug logs confirm our own private server was exercised (BinPetsShop entered, adoption POSTs).

================================================================================
17. TOMORROW'S EXACT STARTING POINT
================================================================================
WHEN WE RESUME TOMORROW, START HERE:

State right now: Backend checkpoints A/B/C are solid; Checkpoint D is PARTIAL (D8/D11 NOT done despite
ROADMAP.md). Bin Pets is PARTWAY: four PHP endpoints + internal.php binPetShop branch exist and are
syntax-checked, but the feature is BLOCKED because (a) no binPetShop catalog rows exist and (b) adoptPet.php
rejects every real item. The TCP server + client were proven working (BinPetsShop room entered live; adoption
POSTs captured). No pet is confirmed stored.

Last thing completed: the bowl/bed COLOUR-MATCHING logic in php2/pets/buy.php (BOWL_BASE=2625 /
BED_BASE=2855, lines ~38-52) — so an adoption grants the player's chosen per-colour pet bowl AND the
matching-colour Bin Pet Bed (2625->2855 ... 2633->2863), with a Blue-bed fallback. Also wrote the four
php2/pets/*.php endpoints + internal.php binPetShop branch, and captured live adoption POSTs in
buy_post_debug.log (proving the SWF reaches the server).

Last thing being worked on: we had JUST FINISHED the colour work and were ABOUT TO RUN A LIVE BIN PETS
TEST (adopt a pet in-client and confirm it stores/renders). The test had not yet been executed when the
session ended — so the colour code is unverified end-to-end, and the test is the immediate next step.

Exact file/function to resume on:
  - game-full/essential/internal.php  -> function getNestShopItems() (line 990) — needs binPetShop seed data
    to return a non-empty catalog.
  - game-full/php2/pets/adoptPet.php  -> line 37 item-shopType check — must align with real SWF item ids.
  - (decide canonical endpoint) game-full/php2/pets/buy.php vs adoptPet.php.

Next immediate action (the live test we were about to run):
  1. Launch XAMPP (Apache+MySQL) + node Main.js, then open the Electron client and walk into BinPetsShop.
  2. Attempt a live adoption via the petBuilder SWF and watch php2/pets/buy_post_debug.log +
     php2/pets/adoptPet.php behaviour.
  3. Use XAMPP php/mysql binaries (C:\xampp\php\php.exe, C:\xampp\mysql\bin\mysql.exe — NOT on MSYS PATH)
     to SELECT * FROM pets and confirm whether a row was inserted.
  4. EXPECTED: the test will surface the blockers below (empty binPetShop catalog -> shop shows nothing;
     adoptPet.php:37 rejects items not shopType='binPetShop'). When it does, pivot to fixing those:
       - inspect what shopType itemTypeID 2846 (and real pet items) actually use;
       - seed/repint binPetShop catalog so getNestShopItems('','binPetShop') returns items;
       - align adoptPet.php's item check (or change canonical endpoint to buy.php).
  5. Confirm the SWF renders the adopted pet + the matching-colour bowl/bed granted by the colour logic.

MUST NOT REDO:
  - Do NOT re-implement auth/hashing/rate-limit/checkpoints A/B/C. They are done and verified.
  - Do NOT touch the pristine (2)/(3) copies or zips.
  - Do NOT rewrite the moderation panel (848fjogfndsl) or electron/main.js.

MUST NOT CHANGE (without reason):
  - The `pets`/`petacquiredskills` table schema (only add seed rows).
  - server/Weevil.js/BinWeevils.js TCP logic unless adding pet sync later.

NEEDS MANUAL TESTING (in client):
  - Bin Pet Shop shows adoptable pets.
  - Adoption completes and pet is listed in profile / pet view.
  - Feed/play/train updates pet stats and persists.
  - Pet name validation rejects duplicates/short names in-client.

REMAINS BEFORE BIN PETS COMPLETE:
  - Catalog seed + endpoint alignment (P1/P2/P3).
  - Confirmed insert + client render.
  - Skills population (petAcquiredSkills) if in scope.
  - Optional: in-world pet sync over TCP.

COMES AFTER BIN PETS:
  - Deferred Checkpoint D gaps (D8 unified moderation, D11 movement validation, D12/D13).
  - Room events (developer-supplied code).
  - Bin Pets Paradise rooms wiring.
  - main.swf audit/cleanup; Extra Bin Pets / nest floors.

================================================================================
18. ONE-PAGE EXECUTIVE SUMMARY
================================================================================
PROJECT:  Bin Weevils Private Server (KnowYourKnot source, Jul-2021 baseline), active working copy
          C:\Users\pc\Desktop\Project Binweevils\Binweevils-main (1)\Binweevils-main\

SOURCE:   KnowYourKnot/Binweevils, used 1:1. Pristine refs (2)/(3)+3 zips UNTOUCHED.
          NOT derived from C:\Users\pc\Desktop\Binweevils (separate git repo) or Binweevils-OG.

CURRENT STATUS: Backend A/B/C done & verified; D partial (D8/D11 NOT done despite ROADMAP.md claim).
          Bin Pets partially implemented and BLOCKED on empty shop catalog.

BACKEND:  A1–A11 implemented (incl. A7/A8 rate limits). B softDrop done. C chat done.
          D: modToken panel lock done, D9 packet history done, D10 partial, D8/D11 NOT done.

LOCALHOST: XAMPP (Apache+MySQL) + node Main.js (:9339/:2087 + WS :10843) + Electron/PepperFlash all
          RUNNING. Dev walked a weevil into BinPetsShop live (roomchange_debug.log). Registration/login/
          game launch/rooms WORK. Bin Pet shop UI loads; adoption POSTs captured.

BIN PETS: 4 PHP endpoints (adoptPet/buy/feedPet/validate-pet-name) + internal.php binPetShop branch,
          syntax-checked. BLOCKED: zero binPetShop itemtype rows => empty shop; adoptPet.php rejects all
          items (res=998). No confirmed pet insert. No TCP pet sync. No skills population.

LAST COMPLETED: Bowl/bed COLOUR-MATCHING in buy.php (per-colour bowl 2625-2633 -> matching bed 2855-2863,
          Blue fallback) + the 4 pet PHP endpoints; were about to LIVE-TEST Bin Pets (test not yet run).

CURRENT TASK: Run the live Bin Pets test we were about to start; it will expose the empty-catalog /
          item-rejection blockers; then fix them so adoption inserts a `pets` row and the pet renders.

NEXT ACTION: Launch client -> BinPetsShop -> adopt; watch buy_post_debug.log; SELECT FROM pets via XAMPP
          mysql. Fix empty binPetShop catalog + adoptPet item check; confirm pet + matched-colour
          bowl/bed appear. Pick ONE canonical adoption endpoint (adoptPet.php vs buy.php).

KNOWN BLOCKERS: (P1) empty binPetShop catalog; (P2) adoptPet rejects real items; (P3) two divergent
          adoption endpoints; (P4) stray "NUL" file; (P5) ROADMAP.md over-claims D8/D11.

DEFERRED: D8/D11/D12/D13, room events (dev-owned), main.swf cleanup, Extra Bin Pets/nest floors,
          Bin Pets Paradise wiring, deployment.

DO NOT REDO: Auth/checkpoints A/B/C; moderation panel; electron/main.js; pristine copies/zips;
          `pets` table schema (add seed rows only).

================================================================================
END OF HANDOFF — no code was modified, committed, or deleted to produce this report.
